VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Web Push: run `node scripts/generate-vapid-keys.mjs` to generate your own
# pair, then set the private half as a Supabase Edge Function secret (see
# supabase/functions/notify-expense/README.md) -- never put it here.
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
